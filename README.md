# Travel Plan Web v2.43.29

## v2.43.29 说明

- 修复移动端“当前计划”下拉打开后出现原生灰色选项层、和右侧按钮视觉挤压的问题。
- 当前计划切换改为自定义轻量下拉菜单，不再触发浏览器原生 select 弹层。
- 移动端继续保持“计划名称 + 全部折叠 + 全部展开 + 查询”在同一行，三个图标按钮紧凑并靠右。
- PC 端同步使用新的计划选择样式；折叠 / 展开保存逻辑不变。

## 部署说明

- GitHub Pages 需要更新：`index.html`、`app.js`、`style.css`、`README.md`、`CHANGELOG.md`。
- Cloudflare Worker 建议更新：`worker.js`，本次主要同步版本号。
- `data.json` 只在你确认线上数据没有更新时再覆盖；线上数据较新时不要直接覆盖。
