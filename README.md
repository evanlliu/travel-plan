# Travel Plan Web v2.43.1

一个轻量级行程计划网页，支持 PC、移动端 Safari、iPhone 添加到主屏幕后使用。

## 功能

- 中英文切换
- 按日期自动分组展示行程
- 日期选择后自动带出星期
- 新增、编辑、删除行程
- 参与人员多选和人员配置
- 小红书链接弹窗预览
- Excel 导入
- 中文 / 英文 Excel 模板导出
- Cloudflare Worker + GitHub `data.json` 多设备同步
- 普通 Safari 和添加到主屏幕后 PWA 模式适配

## 文件说明

```text
index.html              页面结构
style.css               样式和 PC / 移动端适配
app.js                  前端逻辑
data.json               行程数据、人员配置、Cloudflare 配置
worker.js               Cloudflare Worker 同步接口
wrangler.toml           Worker 配置示例
manifest.webmanifest    PWA 配置
icon-192.png            PWA 图标
icon-512.png            PWA 图标
apple-touch-icon.png    iOS 主屏幕图标
```

## GitHub Pages 部署

把以下文件放到 GitHub Pages 仓库根目录：

```text
index.html
style.css
app.js
data.json
manifest.webmanifest
icon-192.png
icon-512.png
apple-touch-icon.png
```

浏览器访问你的 GitHub Pages 地址即可。

## Cloudflare Worker 配置

Worker 需要部署：

```text
worker.js
wrangler.toml
```

Cloudflare Worker 的 Variables and Secrets 建议这样配置：

| Type | Name | Value |
|---|---|---|
| Secret | APP_PASSWORD | 你的写入密码 |
| Plaintext | DATA_PATH | data.json |
| Plaintext | GH_BRANCH | main |
| Plaintext | GH_OWNER | GitHub 用户名 |
| Plaintext | GH_REPO | GitHub 仓库名 |
| Secret | GH_TOKEN | GitHub Personal Access Token |

`APP_PASSWORD` 也会通过前端 Cloudflare 同步配置保存到 `data.json` 的：

```text
settings.cloudflare.appPassword
```

这样新设备加载 `data.json` 后，也能读取 Worker 地址和密码配置。

## 使用说明

1. 打开网页。
2. 点右下角 `+` 新增安排。
3. 点右下角语言按钮切换中英文。
4. 点“更多功能”可以导入 Excel、导出模板、配置人员、配置 Cloudflare。
5. 配置 Cloudflare 后，新增、编辑、删除、人员配置都会同步写入 GitHub 的 `data.json`。

## v2.43.1 界面优化

- PC 端改为更紧凑的行程表格：减少卡片空白、缩小按钮和标签，让一屏显示更多安排。
- 移动端改为时间轴式紧凑卡片：时间固定在左侧，计划内容优先显示，链接和人员作为辅助信息显示。
- 每天标题增加摘要：时间范围、参与人数、链接数量，方便快速抓重点。
- 空链接 / 空人员在移动端隐藏，减少无效信息占位。
- 小红书链接改为胶囊按钮，避免长链接占用太多宽度。


## v2.43.1 说明

- PC 端整体密度继续压缩：卡片、表格、按钮、人员标签、链接标签都更紧凑。
- 移动端整体内容继续缩小：时间轴行、卡片间距、标签、链接胶囊都更小。
- 编辑弹窗同步压缩间距，但移动端输入框字体保持 16px，避免 iOS Safari 自动放大页面。
- 右下角悬浮按钮继续缩小，减少遮挡内容。


## v2.43.1 说明

- 顶部三个信息卡片进一步压缩：标题卡片、搜索卡片、数据状态卡片更矮。
- 卡片之间的上下间隔减少，让日期行程卡片更靠上。
- PC 端数据状态改为单行显示，减少占用高度。
- 移动端顶部信息继续缩小，优先展示下面的日期行程内容。


## v2.43.1 说明

- 小红书 / Red Note 链接点击后直接新窗口打开。
- 取消 iframe 弹窗预览逻辑，避免弹窗遮挡页面。
- 导入或手动录入的链接会自动使用 `target="_blank"` 和 `rel="noopener noreferrer"`。


## v2.43.1 说明

- 每条行程记录新增 Copy / 复制按钮。
- 按钮位置：Edit / 编辑 后面，Delete / 删除 前面。
- 点击复制后会新增一条相同的行程记录，保留日期、时间、分组、计划内容、小红书链接和参与人员。
- 为了避免三按钮占空间，PC 和移动端的操作按钮都做了紧凑化处理。


## v2.43.1 说明

- 每条记录的 Edit / Copy / Delete 三个按钮强制同一行显示。
- PC 端加宽 Action 列，同时缩小按钮间距和按钮尺寸。
- 移动端也保持三按钮横向排列；极窄屏下允许按钮区域横向微滚动，避免挤到下一行。


## v2.43.1 说明

- 移动端新增 / 编辑页面改成一屏紧凑布局。
- 日期和时间一行显示，星期和参与人员一行显示。
- 计划内容、小红书链接高度压缩，提示说明隐藏。
- 底部 Cancel / Save 改为普通表单底部按钮，不再占用额外滚动空间。
- People 选择改为悬浮在选择框下方，不再把表单撑高。


## v2.43.1 说明

- 移动端新增 / 编辑页面改为真正全屏，而不是小卡片浮层。
- 页面视觉重新优化：顶部、表单、底部按钮分区更清楚。
- 计划内容是核心输入项，已放大高度和字体，让用户更直观看到重点。
- 日期/时间、星期/人员保持双列，减少空间浪费。
- People 下拉继续悬浮，不撑高页面。


## v2.43.1 说明

- 移动端编辑页改为单列全屏表单，每个填写项独占一行。
- 页面高度会被完整利用，不再出现大片空白。
- 计划内容区域会自适应吃掉剩余空间，并保持最大的可编辑区域。
- 默认不滚动；只有极小屏幕或键盘弹出时才允许必要滚动。


## v2.43.1 说明

- 移动端编辑页继续优化全屏填充。
- Plan Content 输入框改为自适应高度，占用主要剩余空间。
- Red Note 输入框也改为自适应高度，并比上一版更高。
- 表单会按屏幕高度自动分配 Plan Content 和 Red Note 的高度，尽量刚好填满整个屏幕。


## v2.43.1 说明

- PC 端卡片之间的间隔继续缩短。
- 顶部标题、搜索、状态卡片之间的距离减少。
- 日期卡片之间的距离减少。
- 行程记录之间的间隔也略微压缩，让一屏显示更多内容。


## v2.43.1 说明

- PC 端顶部三个卡片间隔继续缩小。
- 日期卡片外边距和内边距继续压缩。
- 行程记录行距、行高、按钮尺寸进一步缩小。
- 一屏可见内容更多，重点展示日期卡片和行程内容。


## v2.43.1 说明

- PC 端下面日期卡片之间的间距继续缩小。
- 只重点压缩日期卡片外部间隔，尽量不影响卡片内部行程内容可读性。


## v2.43.1 说明

- 每条行程新增「必做 / 可选」类型。
- 日期卡片顶部会统计当天 Must do / Optional 数量。
- 行程内容前方显示类型标签，不额外增加表格列，节省空间。
- 新增 / 编辑页面增加类型切换按钮。
- Excel 导入模板新增类型列，支持中文「必做 / 可选」和英文「Must do / Optional」。
- 兼容旧 data.json：旧记录没有类型字段时默认按「必做」处理。


## v2.43.1 说明

- 修复 Cloudflare Worker 保存到 GitHub data.json 时漏掉 priority 字段的问题。
- 现在「必做 / 可选」会真正写入远端 data.json。
- 刷新页面或切换设备后，会继续保持之前选择的「必做 / 可选」状态。
- Worker 同时兼容 priority / type / 类型 / 重要程度 / Priority / Type。


## v2.43.1 说明

- 基于 v2.42.0 优化。
- 如果一条行程选择 Optional / 可选，这一整行的文字、标签、人员、链接和按钮颜色都会变成低优先级灰色系。
- Must do / 必做 行保持原来的视觉样式。
- 没有套用 v2.43.1 / v2.44.0 的大范围卡片样式修改。


## v2.43.1 说明

- 基于 v2.42.1 继续优化。
- 人员配置页面新增「显示配置」，支持 PC 和移动端分别控制。
- 可分别控制：
  - 计划内容卡片中的 People 列
  - 日期卡片里的 items 数量行
  - 日期卡片里的时间 / 必做 / 人数摘要行
- 配置保存到 `data.json` 的 `settings.displayOptions`。
- Cloudflare Worker 已兼容 `settings.displayOptions`，多个设备可以同步配置。


## v2.43.1 说明

- 删除 Import Excel / Chinese template / English template 三个按钮和关联导入模板功能。
- 删除 Refresh 按钮功能。
- 新增右侧悬浮「同步数据」按钮，和新增、语言切换放在一起。
- People Settings 改名为 Settings，配置入口统一放在设置里。
- 原有显示配置仍保存到 data.json 的 settings.displayOptions，支持多设备同步。


## v2.43.1 说明

- 修复 Settings / 设置按钮多语言显示。
- 修复设置页面 notice 文案 key 不一致导致的多语言问题。
- 修复设置页面显示配置、保存按钮等文案切换检查。
- 修复悬浮按钮和设置按钮的 title / aria-label 多语言。


## v2.43.1 说明

- 重新基于 v2.42.7 做移动端左滑功能。
- 不修改每条记录的 HTML 结构，也不移动内容区域，确保原来的内容样式完全不变。
- 移动端 Edit / Copy / Delete 默认隐藏，左滑后在每行右侧居中显示。


## v2.43.1 说明

- 继续修复移动端左滑按钮位置问题。
- 新增独立的 swipeActions 浮层按钮，不再复用原来的 actionCell。
- 这样可以保持原卡片内容布局不变，同时让左滑按钮在每条记录右侧垂直居中显示。


## v2.43.1 说明

- 按方案 A 优化移动端左滑交互。
- 当某一行左滑展开 Edit / Copy / Delete 时，右下角新增 / 同步 / 语言悬浮按钮自动隐藏。
- 当左滑行关闭、点击空白区域或右滑收起时，右下角悬浮按钮自动恢复。
- 同一时间仍只允许一行保持展开，避免多个操作区同时显示。


## v2.43.1 说明

- 优化移动端 Safari 添加到主屏幕后顶部安全距离，避免标题被状态栏遮挡。
- 移动端顶部区域改为一行：标题 + 版本号 + More。
- 缩小移动端 More 按钮宽度。
- Data loaded 与 Updated 时间改为同一行显示，减少顶部占用高度。


## v2.43.1 说明

- More 菜单改为 PC / 移动端都悬浮展开，不再把页面内容往下挤。
- More 菜单内按钮宽度与高度缩小，整体更紧凑。
- 移动端顶部区域继续压缩：标题版本行、查询框、Data loaded 行高度更低。
- 查询条件框高度与 Data loaded 行保持一致。
- 移动端 PWA / Safari 顶部安全距离略微上移，避免太靠下。


## v2.43.1 说明

- 移动端顶部位置继续上移，减少顶部空白。
- 更新 viewport，禁止移动端 Safari 双击 / 误触导致页面整体放大。
- 移动端输入框字体统一保持 16px，避免 iOS Safari 聚焦输入框时自动放大页面。


## v2.43.1 说明

- 新增旅行计划大分类：例如土耳其旅游计划、墨西哥旅游计划、中国旅游计划。
- 顶部新增当前分类下拉切换，PC 和移动端都支持。
- More 菜单中新增“旅行计划分类”管理入口。
- 每个分类拥有独立的行程记录、人员列表、显示配置。
- 分类支持：新建、切换、复制、归档、恢复、永久删除已归档分类。
- 所有分类数据保存到 data.json，支持 Cloudflare 多设备同步。


## v2.43.1 重要修复

- 修复 v2.43.0 升级后，打包自带的 data.json 可能覆盖用户现有数据的问题。
- data.json 现在只作为首次安装默认数据使用；如果本地已有用户数据，加载新版时不会用打包 data.json 覆盖。
- 修复分类新增 / 切换 / 保存时，当前分类数据没有稳定写入 trips 的问题。
- 增加本地备份 localStorage: travel-plan-local-data-backup，降低误覆盖风险。
- 注意：如果 GitHub 上已有真实 data.json，升级时不要上传压缩包里的默认 data.json 覆盖它。
