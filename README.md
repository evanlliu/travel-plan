# Travel Plan Web v2.43.23

## v2.43.23 说明

- 首次打开页面和点击悬浮同步按钮时，版本号后面先显示 `Loading data...`。
- 实际获取到 `data.json` 数据并完成页面渲染后，再显示 `Synced HH:mm:ss`。
- `HH:mm:ss` 使用本次实际加载完成/刷新完成的当前时间，不再显示旧缓存时间。
- 手动悬浮同步刷新只拉取最新 `data.json` 并渲染，避免本地旧数据覆盖云端最新数据。

